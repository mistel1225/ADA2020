#include <iostream>
#include <vector>
#include "helper.h"
using namespace std;
using namespace Memory;

int main(){
	int N, M;
	cin>>N>>M;
	vector<vector<long long>> damage_record(M, vector<long long>(2));
	string in;
	long long c, l, r, d, p, k;
	if(M<=N){
		for(int i=0; i<N; i++){
			cin>>in;
			if(in=="p"){
				cin>>c>>d;
			}
				
		}
	}
	else{
		
	}
}
